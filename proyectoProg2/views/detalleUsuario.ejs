<!DOCTYPE html>
<html lang="en">

    <%- include('./partials/head')  %> 

<body>
    <%- include('./partials/header')  %> 

    <main>
        <div class="user-detail container">
            <section class="top main-content">
                <img class="profile" src="/img/usuarios/<%= detalle.img %> " alt="">
                <div class="info-usuario">
                    <p><%= detalle.nombre %> </p> 
                    <a class="follow" href="/users/editarPerfil/0"><i class="fa-solid fa-user-gear"></i></a>
                    <ul class="meta">
                        <li>Posteos: <%= detalle.posteos.length %> </li>
                        <li>Seguidores: <%= detalle.misSeguidores.length %> </li>
                        <li>Seguidos: <%= detalle.misSeguidos.length %> </li>
                    </ul>
                </div>
            </section>

            <section class="bottom">
                <% for(let i = 0; i < detalle.posteos.length; i++) { %>
                <article class="post">
                    <a href="/post/detallePost/<%= detalle.posteos[i].id %> ">
                        <img src="/img/post/<%= detalle.posteos[i].imagen %> " alt="">
                        <p><%= detalle.posteos[i].comentario %> </p>
                    </a>
                </article>
                <% } %>
            </section>
        </div>
    </main>

    <%- include('./partials/footer')  %> 
</body>

</html>